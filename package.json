{
  "name": "atelier-thread-saas",
  "version": "2.0.0",
  "private": true,
  "description": "Production-ready multi-tenant SaaS AI-powered custom fashion e-commerce platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -k \"npm run dev --workspace @atelier/api\" \"npm run dev --workspace @atelier/web\" \"npm run dev:worker --workspace @atelier/api\"",
    "dev:api": "npm run dev --workspace @atelier/api",
    "dev:web": "npm run dev --workspace @atelier/web",
    "dev:worker": "npm run dev:worker --workspace @atelier/api",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "db:migrate": "npm run db:migrate --workspace @atelier/api",
    "db:seed": "npm run db:seed --workspace @atelier/api",
    "docker:up": "docker compose -f docker-compose.saas.yml up --build",
    "docker:down": "docker compose -f docker-compose.saas.yml down",
    "legacy:dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "legacy:dev:with-worker": "concurrently \"npm run dev --prefix backend\" \"npm run worker:orders --prefix backend\" \"npm run dev --prefix frontend\"",
    "legacy:build": "npm run build --prefix frontend"
  },
  "engines": {
    "node": ">=20.11.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
